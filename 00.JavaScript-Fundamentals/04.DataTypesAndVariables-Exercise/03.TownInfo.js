function townInfo(town, population, area) {
    let output = `Town ${town} has population of ${population} and area ${area} square km.`
    console.log(output);
}

townInfo('Sofia', '1286383', '492');